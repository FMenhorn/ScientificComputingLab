%% Worksheet 1
%<<<<<<< HEAD
% a) Use matlab to plot the function p(t) in a graph.

t = 0:0.1:9.9;
%p = 10./(1+9.*exp(-t));

%plot(t,p)
%% b)
%1)
diff_func = @(p)(1-(p/10))*p;
dt = 0.1;
t_end = 10;
y0 = 1;
y = Euler(diff_func,y0,dt,t_end);

size(t)
size(y)
plot(t,y)

%2)

%% 


%=======
% a) use matlab to plot the function 
%t=(1:0.1:10);

%p=10./(1+9.^-t);

%plot(t,p)
%>>>>>>> origin/master
